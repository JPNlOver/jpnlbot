const Discord = require('discord.js');


const client = new Discord.Client();
const fs = require('fs')
var config = JSON.parse(fs.readFileSync('./botsettings.json', 'utf-8'));


const token = 'NDQ4ODU3ODcwNDI0MDgwMzk0.DhnQFw.vO9NCNTw4Gci0DBaORMuSm5YjXo';

client.on('message', message => {

  if (message.content === '<3clear') {
    message.channel.send(":exclamation: Expecifique quantas mensagens devem ser apagadas!")
  }
  
  if (message.content === '<3clear 2') {

    let messagecount = parseInt(2);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 3') {

    let messagecount = parseInt(3);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 4') {

    let messagecount = parseInt(4);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 5') {

    let messagecount = parseInt(5);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 6') {

    let messagecount = parseInt(6);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 7') {

    let messagecount = parseInt(7);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 8') {

    let messagecount = parseInt(8);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 9') {

    let messagecount = parseInt(9);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 10') {

    let messagecount = parseInt(10);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 11') {

    let messagecount = parseInt(11);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 12') {

    let messagecount = parseInt(12);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 13') {

    let messagecount = parseInt(13);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 14') {

    let messagecount = parseInt(14);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 15') {

    let messagecount = parseInt(15);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 16') {

    let messagecount = parseInt(16);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 17') {

    let messagecount = parseInt(17);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 18') {

    let messagecount = parseInt(18);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 19') {

    let messagecount = parseInt(19);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 20') {

    let messagecount = parseInt(20);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 21') {

    let messagecount = parseInt(21);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 22') {

    let messagecount = parseInt(22);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 23') {

    let messagecount = parseInt(23);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 24') {

    let messagecount = parseInt(24);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 25') {

    let messagecount = parseInt(25);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 26') {

    let messagecount = parseInt(26);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 27') {

    let messagecount = parseInt(27);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 28') {

    let messagecount = parseInt(28);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 29') {

    let messagecount = parseInt(29);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 30') {

    let messagecount = parseInt(30);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 31') {

    let messagecount = parseInt(31);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 32') {

    let messagecount = parseInt(32);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 33') {

    let messagecount = parseInt(33);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 34') {

    let messagecount = parseInt(34);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 35') {

    let messagecount = parseInt(35);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 36') {

    let messagecount = parseInt(36);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 37') {

    let messagecount = parseInt(37);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 38') {

    let messagecount = parseInt(38);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 39') {

    let messagecount = parseInt(39);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 40') {

    let messagecount = parseInt(40);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 41') {

    let messagecount = parseInt(41);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 42') {

    let messagecount = parseInt(42);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 43') {

    let messagecount = parseInt(43);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 44') {

    let messagecount = parseInt(44);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 45') {

    let messagecount = parseInt(45);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 46') {

    let messagecount = parseInt(46);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 47') {

    let messagecount = parseInt(47);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 48') {

    let messagecount = parseInt(48);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 49') {

    let messagecount = parseInt(49);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 50') {

    let messagecount = parseInt(50);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 51') {

    let messagecount = parseInt(51);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 52') {

    let messagecount = parseInt(52);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
   if (message.content === '<3clear 53') {

    let messagecount = parseInt(53);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 54') {

    let messagecount = parseInt(54);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 55') {

    let messagecount = parseInt(55);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  
  if (message.content === '<3clear 56') {

    let messagecount = parseInt(56);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  if (message.content === '<3clear 57') {

    let messagecount = parseInt(57);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  if (message.content === '<3clear 58') {

    let messagecount = parseInt(58);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  if (message.content === '<3clear 59') {

    let messagecount = parseInt(59);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  if (message.content === '<3clear 60') {

    let messagecount = parseInt(60);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 61') {
    let messagecount = parseInt(61);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 62') {
    let messagecount = parseInt(62);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 63') {
    let messagecount = parseInt(63);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 64') {
    let messagecount = parseInt(64);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }  if (message.content === '<3clear 65') {
    let messagecount = parseInt(65);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 66') {
    let messagecount = parseInt(66);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 67') {
    let messagecount = parseInt(67);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 68') {
    let messagecount = parseInt(68);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 69') {
    let messagecount = parseInt(69);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 70') {
    let messagecount = parseInt(70);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 71') {
    let messagecount = parseInt(71);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 72') {
    let messagecount = parseInt(72);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 73') {
    let messagecount = parseInt(73);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 74') {
    let messagecount = parseInt(74);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 75') {
    let messagecount = parseInt(75);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 76') {
    let messagecount = parseInt(76);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 77') {
    let messagecount = parseInt(77);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 78') {
    let messagecount = parseInt(78);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 79') {
    let messagecount = parseInt(79);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 80') {
    let messagecount = parseInt(80);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 81') {
    let messagecount = parseInt(81);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 82') {
    let messagecount = parseInt(82);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 83') {
    let messagecount = parseInt(83);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 84') {
    let messagecount = parseInt(84);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 85') {
    let messagecount = parseInt(85);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 86') {
    let messagecount = parseInt(86);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 87') {
    let messagecount = parseInt(87);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 88') {
    let messagecount = parseInt(88);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 89') {
    let messagecount = parseInt(89);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 90') {
    let messagecount = parseInt(90);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 91') {
    let messagecount = parseInt(91);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 92') {
    let messagecount = parseInt(92);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 93') {
    let messagecount = parseInt(93);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 94') {
    let messagecount = parseInt(94);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 95') {
    let messagecount = parseInt(95);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 96') {
    let messagecount = parseInt(96);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 97') {
    let messagecount = parseInt(97);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 98') {
    let messagecount = parseInt(98);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 99') {
    let messagecount = parseInt(99);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
    if (message.content === '<3clear 100') {
    let messagecount = parseInt(100);
  message.channel.fetchMessages({limit: messagecount}).then(messages => message.channel.bulkDelete(messages));
  }
  

	 });


client.login(config.token);

module.exports.help = {
    name: "icon",
}